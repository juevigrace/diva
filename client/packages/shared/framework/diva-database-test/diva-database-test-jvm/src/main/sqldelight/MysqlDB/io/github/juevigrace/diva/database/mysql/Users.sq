insertUser:
INSERT INTO users(name, email, created_at)
VALUES (?, ?, ?);

selectAll:
SELECT * FROM users;

selectById:
SELECT * FROM users
WHERE id = ?;

selectByEmail:
SELECT * FROM users
WHERE email = ?;

deleteById:
DELETE FROM users
WHERE id = ?;