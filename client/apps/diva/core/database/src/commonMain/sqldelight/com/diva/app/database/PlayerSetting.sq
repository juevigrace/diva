upsert:
INSERT OR REPLACE INTO diva_player_setting (id, key, value, updated_at)
VALUES (?, ?, ?, ?);

deleteByKey:
DELETE FROM diva_player_setting
WHERE key = ?;

findAll:
SELECT *
FROM diva_player_setting;

findByKey:
SELECT value
FROM diva_player_setting
WHERE key = ?;