// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: device.sql

package pg

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listAllDevices = `-- name: ListAllDevices :many
select
    d.id,
    d.name,
    d.created_at,
    d.updated_at
from diva_devices d
order by d.name asc
`

func (q *Queries) ListAllDevices(ctx context.Context) ([]DivaDevice, error) {
	rows, err := q.db.Query(ctx, listAllDevices)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DivaDevice
	for rows.Next() {
		var i DivaDevice
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const createDevice = `-- name: CreateDevice :exec
insert into diva_devices (
    id,
    name
) values (
    $1,
    $2
)
`

type CreateDeviceParams struct {
	ID   pgtype.UUID
	Name string
}

func (q *Queries) CreateDevice(ctx context.Context, arg CreateDeviceParams) error {
	_, err := q.db.Exec(ctx, createDevice, arg.ID, arg.Name)
	return err
}

const getDeviceByID = `-- name: GetDeviceByID :one
select
    d.id,
    d.name,
    d.created_at,
    d.updated_at
from diva_devices d
where d.id = $1
`

func (q *Queries) GetDeviceByID(ctx context.Context, id pgtype.UUID) (DivaDevice, error) {
	row := q.db.QueryRow(ctx, getDeviceByID, id)
	var i DivaDevice
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDeviceByName = `-- name: GetDeviceByName :one
select
    d.id,
    d.name,
    d.created_at,
    d.updated_at
from diva_devices d
where d.name = $1
`

func (q *Queries) GetDeviceByName(ctx context.Context, name string) (DivaDevice, error) {
	row := q.db.QueryRow(ctx, getDeviceByName, name)
	var i DivaDevice
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
