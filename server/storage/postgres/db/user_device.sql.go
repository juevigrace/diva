// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: user_device.sql

package pg

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createUserDevice = `-- name: CreateUserDevice :exec
insert into diva_user_device (
    user_id,
    device_id
) values (
    $1,
    $2
)
`

type CreateUserDeviceParams struct {
	UserID   pgtype.UUID
	DeviceID pgtype.UUID
}

func (q *Queries) CreateUserDevice(ctx context.Context, arg CreateUserDeviceParams) error {
	_, err := q.db.Exec(ctx, createUserDevice, arg.UserID, arg.DeviceID)
	return err
}

const deleteUserDevice = `-- name: DeleteUserDevice :exec
delete from diva_user_device
where user_id = $1 and device_id = $2
`

type DeleteUserDeviceParams struct {
	UserID   pgtype.UUID
	DeviceID pgtype.UUID
}

func (q *Queries) DeleteUserDevice(ctx context.Context, arg DeleteUserDeviceParams) error {
	_, err := q.db.Exec(ctx, deleteUserDevice, arg.UserID, arg.DeviceID)
	return err
}

const getUserDevice = `-- name: GetUserDevice :one
select
    ud.user_id,
    ud.device_id,
    ud.created_at,
    ud.updated_at
from diva_user_device ud
where ud.user_id = $1 and ud.device_id = $2
`

type GetUserDeviceParams struct {
	UserID   pgtype.UUID
	DeviceID pgtype.UUID
}

func (q *Queries) GetUserDevice(ctx context.Context, arg GetUserDeviceParams) (DivaUserDevice, error) {
	row := q.db.QueryRow(ctx, getUserDevice, arg.UserID, arg.DeviceID)
	var i DivaUserDevice
	err := row.Scan(
		&i.UserID,
		&i.DeviceID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listUserDevices = `-- name: ListUserDevices :many
select
    ud.user_id,
    ud.device_id,
    ud.created_at,
    ud.updated_at
from diva_user_device ud
where ud.user_id = $1
order by ud.updated_at desc
`

func (q *Queries) ListUserDevices(ctx context.Context, userID pgtype.UUID) ([]DivaUserDevice, error) {
	rows, err := q.db.Query(ctx, listUserDevices, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DivaUserDevice
	for rows.Next() {
		var i DivaUserDevice
		if err := rows.Scan(
			&i.UserID,
			&i.DeviceID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
